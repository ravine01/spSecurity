<?xml version="1.0" encoding="UTF-8"?>
<b:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:b="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">

	<http auto-config="true">
		<intercept-url pattern="/" access="permitAll" />
		<intercept-url pattern="/admin/**" access="hasRole('ADMIN')" />
		<intercept-url pattern="/guest/**" access="hasRole('GUEST')" />
		<form-login authentication-failure-url="/failLogin" />

		<remember-me  services-ref="rememberMeServices" />
		
		<logout delete-cookies="JSESSIONID"/>

	</http>

	<b:bean id="userDetailsService" class="org.zerock.security.UserServiceImpl">
	</b:bean>

	<b:bean id="rememberMeServices"
		class="org.springframework.security.web.authentication.rememberme.PersistentTokenBasedRememberMeServices">
		<b:constructor-arg value="myAppKey" />
		<b:constructor-arg ref="userDetailsService" />
		<b:constructor-arg ref="tokenRepository" />
		<b:property name="cookieName" value="remember-me" />
		<b:property name="tokenValiditySeconds" value="300" />
		<b:property name="parameter" value="remember-me-parameter" />
	</b:bean>
	
	 <b:bean id="tokenRepository"
          class="org.springframework.security.web.authentication.rememberme.InMemoryTokenRepositoryImpl">
    </b:bean>



	<b:bean id="zerockSecurity" class="org.zerock.security.ZerockUserAuthProvider">
	</b:bean>

	<authentication-manager>
		<authentication-provider ref="zerockSecurity">

		</authentication-provider>

	</authentication-manager>
</b:beans>